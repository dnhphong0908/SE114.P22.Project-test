package com.se114p12.backend.enums;

public enum PaymentStatus {
    PENDING,
    COMPLETED,
    FAILED,
    CANCELED,
    INVALID_AMOUNT,
    ORDER_NOT_FOUND,
    ALREADY_CONFIRMED,
    INVALID_CHECKSUM,
    UNKNOWN_ERROR
}
